{
  "name": "extra-miles-pos-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "ts-node-dev --respawn --transpile-only index.ts",
    "client": "react-scripts start",
    "dev": "concurrently \" cd server && npm run server\" \"cd client && npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.3.0"
  }
}
